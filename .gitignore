# Variables d'environnement — JAMAIS committer la clé privée du wallet
.env
.env.local
.env.*.local

# Dépendances Node.js
node_modules/

# Artefacts de compilation Hardhat
artifacts/
cache/
# typechain-types/ are committed so Railway/CI can build the API without running hardhat

# Rapports de coverage
coverage/
coverage.json

# Fichiers système
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# IDE
.vscode/settings.json

# SDK build output
sdk/dist/

# API local data — contains encrypted private keys of test agents
api/data/

# Claude Code configuration — not for public repo
.claude/
CLAUDE.md
sdk/python/dist/
sdk/python/asp_sdk.egg-info/
sdk/python/build/
api/dist/
