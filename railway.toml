[build]
buildCommand = "cd api && npm ci && npm run build"

[deploy]
startCommand = "cd api && node dist/api/src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
